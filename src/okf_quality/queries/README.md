# SPARQL query packs (P2)

| Directory | Use |
|-----------|-----|
| `integrity/` | Non-empty results ⇒ findings (CI may fail) |
| `inventory/` | Counts / exploration (informational) |

Run via `python -m okf_quality.scripts.run_queries`.

Prefixes match `okf2rdf` export (`okf:`, `schema:`, `dcterms:`).
