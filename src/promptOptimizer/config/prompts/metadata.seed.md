Produce schema.org Dataset metadata as JSON-LD for the described dataset,
aligned with the NIAID Blueprint for Digital Objects.

Emit a single well-formed JSON-LD record in a fenced code block, grounded in
the scenario. Use realistic values and persistent identifiers where they
apply — not placeholders or generic templates. Brief notes after the block
are fine; keep the record itself complete.
